
services:
  nginx:
    image: samdox/custom-nginx:latest
    container_name: nginx
    ports:
      - "80:80"
      - "443:443"
    networks:
      - sam-network

networks:
  sam-network:
    external: true